name: Nightly (disabled in fork)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Nightly disabled
        run: echo "Nightly release workflow is disabled in this custom fork."
